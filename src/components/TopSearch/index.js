import React from 'react';
import Heading from 'components/Heading';

import { Col, Row } from 'antd';

const TopSearch = () => {
  return (
    <div></div>
    // <div style={{ padding: 25, background: '#EEEEEE' }}>
    //   <Heading isLeft>TÌM KIẾM NHIỀU NHẤT</Heading>
    //   <Row gutter={24}>
    //     {[...new Array(5)].map((elm, idx) => (
    //       <Col span={7} key={idx}>
    //         Áo mắt chim
    //       </Col>
    //     ))}
    //   </Row>
    // </div>
  );
};

export default TopSearch;
